import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mopmanual.app',
  appName: 'MOP Manual',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
