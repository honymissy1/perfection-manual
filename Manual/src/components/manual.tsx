import './ExploreContainer.css';


interface ContainerProps {
  name: string;
}

const Manual: React.FC<ContainerProps> = ({ name }) => {
  return (
    <div className="container">
        
    </div>
  );
};

export default Manual;
